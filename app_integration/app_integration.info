name = App Integration Example
description = An example module to demonstrate options for adding javascript based visualizations to wri.org (or any Drupal site). The module demonstrates how to embed a D3 based visualization as a Drupal node (page) and as a Drupal block (page element). Server-side processing takes place in the module as PHP and then packages up the output as a node (page) and also as a block (page element).  
version = 7.x-1-beta
core = 7.x  
package = WRI

